{
  "name": "sns-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel run dev",
    "build": "pnpm --recursive run build",
    "lint": "pnpm --recursive run lint",
    "prepush": "pnpm audit --audit-level=high"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
